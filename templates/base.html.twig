<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="icon" href="{{ asset('build/images/logo_sm_light.ico') }}">
        <title>
            {% block title %}Portfolio PAUQUIN
            {% endblock %}
        </title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    <body>
    {% if app.request.get('_route') not in ['app_login'] %}
        {% include 'navBar/_navbar.html.twig' %}
    {% endif %}
          {% block body %}{% endblock %}
    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
        {{ encore_entry_script_tags('main') }}
    {% endblock %}
    </body>
</html>
